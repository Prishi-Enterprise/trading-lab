@AGENTS.md

Playbooks for common tasks: `skills/gold-tracker/SKILL.md`. Run `./scripts/link_skills.sh` once
after cloning to expose it as a project skill in `.claude/skills/` (Claude Code / Cowork) and
`.cursor/skills/` (Cursor). In any other tool, just read the file.
